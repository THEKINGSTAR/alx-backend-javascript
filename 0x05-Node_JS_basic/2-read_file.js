function countStudents(path) {
  if (!path || path == null) {
    console.log('Cannot load the database');
  }
  else
  {
    const inFile = FileReader(path);
    
  }
}

module.exports = countStudents;
countStudents();
